@model OpticApp.Models.EtatVisite

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>EtatVisite</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ID" />
            <div class="form-group">
                <label asp-for="libelleEtatVisite" class="control-label"></label>
                <input asp-for="libelleEtatVisite" class="form-control" />
                <span asp-validation-for="libelleEtatVisite" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Retour à la Liste</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
